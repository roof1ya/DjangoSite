document.addEventListener("DOMContentLoaded", function () {
    // Каждые 5 секунд обновлять страницу полностью
    setInterval(function () {
        location.reload();  // Эта команда просто обновляет страницу через заданный интервал
    }, 15000);
});